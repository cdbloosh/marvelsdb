services:
  - type: web
    name: marvelcdb
    env: php
    phpVersion: 8.1
    buildCommand: "curl -sS https://getcomposer.org/installer | php && php composer.phar install --no-dev"
    startCommand: "php artisan serve --host 0.0.0.0 --port $PORT"

